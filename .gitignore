# OS
.DS_Store

# Tooling (if a linter/bundler is added later)
node_modules/
dist/
*.log

# Generated clients never live here — they are generated into the consuming repos.
# If a bundled/dereferenced spec is ever emitted, ignore it:
/build/
/generated/
